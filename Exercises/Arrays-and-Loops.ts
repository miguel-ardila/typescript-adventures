// Descripción: Crear un arreglo y utilizar un bucle for para imprimir sus elementos.
// Declaración de un arreglo
const colores: string[] = ["Rojo", "Verde", "Azul"];

// Iteración a través del arreglo
for (let i = 0; i < colores.length; i++) {
    console.log(`Color ${i + 1}: ${colores[i]}`);
}
